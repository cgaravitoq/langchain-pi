{
  "name": "langchain-pi-sidecar",
  "private": true,
  "type": "module",
  "description": "Node deps for the langchain-pi sidecar (not published; the Python package on PyPI is 'langchain-pi').",
  "dependencies": {
    "@earendil-works/pi-ai": "^0.78.0",
    "@earendil-works/pi-coding-agent": "^0.78.0"
  }
}
