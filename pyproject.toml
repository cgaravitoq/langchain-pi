[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "open-langchain"
version = "0.6.1"
description = "Native LangChain chat models for OpenAI Codex subscription OAuth and OpenCode Zen/Go — no Node sidecar."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "cgaravitoq" }]
keywords = ["langchain", "langgraph", "openai", "codex", "opencode", "llm", "chat-model"]
dependencies = ["langchain-core>=0.3.15,<2", "langchain-openai>=0.2", "httpx>=0.27"]

[project.urls]
Homepage = "https://github.com/cgaravitoq/open-langchain"
Repository = "https://github.com/cgaravitoq/open-langchain"

[project.scripts]
codex-login = "open_langchain.cli:main"

[project.optional-dependencies]
test = [
  "langchain-tests>=0.3.5",
  "pytest>=7.4",
  "pytest-asyncio>=0.23",
  "pytest-socket>=0.7",
]

[tool.hatch.build.targets.wheel]
packages = ["open_langchain"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff.lint]
select = ["E", "F", "I", "T201"]
